import Image from "next/image"; import { Container } from "@/components/ui/Container/Container"; import styles from "./ContextualServiceSections.module.css";
export default function ContextualAuditSection({ data }: { data: { title: string; text: string; image: string; checklist: readonly string[] } }) { return <section id="audit" className={`${styles.section} ${styles.lightSection}`}><Container><div className={styles.split}><div><h2>{data.title}</h2><p className={styles.lead}>{data.text}</p><ul className={styles.checklist}>{data.checklist.map(item=><li key={item}>{item}</li>)}</ul></div><Image className={styles.visual} src={data.image} alt="" width={1200} height={900}/></div></Container></section>; }
