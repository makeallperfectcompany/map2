import { Container } from "@/components/ui/Container/Container"; import grid from "@/styles/grid.module.css"; import styles from "./ContextualServiceSections.module.css";
export default function ContextualIndustriesSection({ data }: { data: { title: string; cards: readonly (readonly [string,string])[] } }) { return <section className={`${styles.section} ${styles.lightSection}`}><Container><h2>{data.title}</h2><div className={`${grid.grid3} ${styles.cardGrid}`}>{data.cards.map(([title,text])=><article className={styles.card} key={title}><h3>{title}</h3><p>{text}</p></article>)}</div></Container></section>; }
