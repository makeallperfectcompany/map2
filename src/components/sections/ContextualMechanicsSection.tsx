import Image from "next/image"; import { Container } from "@/components/ui/Container/Container"; import styles from "./ContextualServiceSections.module.css";
export default function ContextualMechanicsSection({ data }: { data: { title: string; text: string; image: string; chips: readonly string[] } }) { return <section className={`${styles.section} ${styles.lightSection}`}><Container><div className={styles.split}><Image className={styles.visual} src={data.image} alt="" width={1400} height={1000}/><div><h2>{data.title}</h2><p className={styles.lead}>{data.text}</p><div className={styles.chips}>{data.chips.map(chip=><span key={chip}>{chip}</span>)}</div></div></div></Container></section>; }
