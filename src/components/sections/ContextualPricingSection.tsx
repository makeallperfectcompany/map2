import { Container } from "@/components/ui/Container/Container"; import styles from "./ContextualServiceSections.module.css";
export default function ContextualPricingSection({ data }: { data: { title: string; text: string; price: string; note: string; chips: readonly string[] } }) { return <section className={styles.section}><Container><div className={styles.split}><aside className={styles.priceCard}><p>Стоимость работ</p><strong>{data.price}</strong><span>{data.note}</span><a href="#contact">Рассчитать стоимость</a></aside><div><h2>{data.title}</h2><p className={styles.lead}>{data.text}</p><div className={styles.chips}>{data.chips.map(chip=><span key={chip}>{chip}</span>)}</div></div></div></Container></section>; }
