import { Container } from "@/components/ui/Container/Container";
import { ormContent } from "@/content/services/orm";
import grid from "@/styles/grid.module.css";
import styles from "./OrmScopeSection.module.css";
export default function OrmScopeSection() { const { scope } = ormContent; const [audit, monitor, negative, trust, control] = scope.items; return <section className={styles.section}><Container><div className={styles.header}><h2>{scope.title}</h2><p>{scope.description}</p></div><div className={`${grid.gridAsymmetric} ${styles.top}`}><article className={`${styles.card} ${styles.feature}`}><span>01</span><h3>{audit.title}</h3><p>{audit.text}</p></article><div className={styles.stack}><article className={styles.card}><span>02</span><h3>{monitor.title}</h3><p>{monitor.text}</p></article><article className={styles.card}><span>03</span><h3>{negative.title}</h3><p>{negative.text}</p></article></div></div><div className={`${grid.grid2} ${styles.bottom}`}><article className={styles.card}><span>04</span><h3>{trust.title}</h3><p>{trust.text}</p></article><article className={styles.card}><span>05</span><h3>{control.title}</h3><p>{control.text}</p></article></div></Container></section>; }
