.servicesSection {
  position: relative;
  margin: 0 24px;
  border-radius: 32px;
  padding: 72px 32px 64px;
  color: #F5F5F7;
  background: #1D1D1F;
  border: 1px solid rgba(255, 255, 255, 0.08);
}

.container {
  position: relative;
  z-index: 1;
  width: min(100%, 1232px);
  margin: 0 auto;
  padding: 0;
}

.servicesIntro {
  display: grid;
  grid-template-columns: minmax(0, 0.95fr) minmax(360px, 0.52fr);
  align-items: start;
  gap: 64px;
  margin-bottom: 78px;
}

.kicker {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: #6FB7FF;
  font: 780 16px/1 Onest, Inter, system-ui, sans-serif;
  letter-spacing: -0.02em;
}

.kicker::before {
  content: "";
  width: 8px;
  height: 8px;
  border-radius: 2px;
  background: #6FB7FF;
  transform: rotate(45deg);
  box-shadow: 0 0 18px rgba(111, 183, 255, 0.72);
}

.servicesIntro h2 {
  max-width: 720px;
  margin: 28px 0 0;
  font: 800 clamp(46px, 5.8vw, 82px)/0.98 Onest, Inter, system-ui, sans-serif;
  letter-spacing: -0.03em;
  text-wrap: balance;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

.servicesIntro h2 span {
  display: block;
  color: rgba(245, 245, 247, 0.55);
}

.servicesIntro > p {
  max-width: 430px;
  margin: 86px 0 0;
  color: rgba(245, 245, 247, 0.65);
  font: 440 18px/1.55 Onest, Inter, system-ui, sans-serif;
  letter-spacing: -0.02em;
}

.introActions {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 14px;
  margin-top: 34px;
}

.introActions a {
  min-height: 52px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  border-radius: 999px;
  font: 760 15px/1 Onest, Inter, system-ui, sans-serif;
  letter-spacing: -0.01em;
}

.introActions a:first-child {
  padding: 0 25px;
  color: #121820;
  background: #ffffff;
  box-shadow: 0 16px 36px rgba(0, 0, 0, 0.18);
}

.introActions a:last-child {
  padding: 0;
  color: rgba(245, 245, 247, 0.85);
  background: transparent;
  position: relative;
  gap: 0 6px;
}

.introActions a:last-child::after {
  content: '';
  position: absolute;
  left: 0;
  right: 0;
  bottom: 8px;
  height: 1px;
  background: rgba(245, 245, 247, 0.85);
  transform: scaleX(0);
  transform-origin: center;
  transition: transform 0.25s cubic-bezier(0.16, 1, 0.3, 1);
}

.introActions a:last-child:hover { color: #ffffff; }
.introActions a:last-child:hover::after { transform: scaleX(1); }

.servicesGrid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
  align-items: stretch;
}

.serviceCard {
  position: relative;
  min-height: 440px;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  padding: 24px 22px;
  border-radius: 20px;
  background: #2A2A2C;
  border: 1px solid rgba(255, 255, 255, 0.075);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.045),
    0 22px 70px rgba(0, 0, 0, 0.18);
  transition: transform 240ms ease, border-color 240ms ease, box-shadow 240ms ease;
}

.serviceCard:hover {
  transform: translateY(-4px);
  border-color: rgba(111, 183, 255, 0.22);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.06),
    0 30px 86px rgba(0, 0, 0, 0.26);
}

.featuredCard {
  background: linear-gradient(135deg, #1A3A6E 0%, #0A1F3D 100%);
  border-color: rgba(111, 183, 255, 0.18);
}

.cardEyebrow {
  color: rgba(255, 255, 255, 0.42);
  font: 760 12px/1 JetBrains Mono, ui-monospace, SFMono-Regular, Menlo, monospace;
  text-transform: uppercase;
  letter-spacing: 0.12em;
}

.serviceCard h3 {
  margin: 30px 0 0;
  max-width: 430px;
  color: #ffffff;
  font: 850 33px/1.04 Onest, Inter, system-ui, sans-serif;
  letter-spacing: -0.062em;
}

.serviceCard h3 span {
  display: block;
  color: rgba(255, 255, 255, 0.52);
}

.bigMark {
  margin-top: 26px;
  color: #6FB7FF;
  font: 850 72px/0.9 Onest, Inter, system-ui, sans-serif;
  letter-spacing: -0.075em;
}

.featuredCard .bigMark {
  color: #ffffff;
  font-size: 80px;
}

.serviceCard p {
  max-width: 440px;
  margin: 24px 0 0;
  color: rgba(255, 255, 255, 0.60);
  font: 430 15px/1.6 Onest, Inter, system-ui, sans-serif;
  letter-spacing: -0.012em;
}

.chips {
  display: flex;
  flex-wrap: wrap;
  align-content: flex-start;
  gap: 8px;
  margin-top: 24px;
  min-height: 78px;
}

.chips span,
.chips .chipLink {
  min-height: 31px;
  display: inline-flex;
  align-items: center;
  padding: 0 10px;
  border-radius: 999px;
  color: rgba(255, 255, 255, 0.78);
  background: rgba(255, 255, 255, 0.066);
  border: 1px solid rgba(255, 255, 255, 0.075);
  font: 650 12px/1 Onest, Inter, system-ui, sans-serif;
  white-space: nowrap;
  text-decoration: none;
  transition: background 0.2s ease, border-color 0.2s ease, color 0.2s ease, transform 0.2s ease;
}

.chips .chipLink:hover {
  background: rgba(255, 255, 255, 0.13);
  border-color: rgba(255, 255, 255, 0.20);
  color: #ffffff;
  transform: translateY(-1px);
}

.chips .chipLink:focus-visible {
  outline: 2px solid #6FB7FF;
  outline-offset: 2px;
}

.featuredCard .chips span,
.featuredCard .chipLink {
  background: rgba(255, 255, 255, 0.09);
  border-color: rgba(255, 255, 255, 0.10);
}

.featuredCard .chipLink:hover {
  background: rgba(255, 255, 255, 0.18);
  border-color: rgba(255, 255, 255, 0.25);
}

.cardBottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  margin-top: auto;
  padding-top: 26px;
  border-top: 1px solid rgba(255, 255, 255, 0.08);
}

.cardBottom span {
  color: rgba(255, 255, 255, 0.42);
  font: 700 13px/1 JetBrains Mono, ui-monospace, SFMono-Regular, Menlo, monospace;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.cardBottom a {
  color: #6FB7FF;
  text-decoration: none;
  font: 780 14px/1 Onest, Inter, system-ui, sans-serif;
  letter-spacing: -0.01em;
}

.featuredCard .cardBottom a {
  color: #ffffff;
}

@media (max-width: 1180px) {
  .servicesIntro {
    grid-template-columns: 1fr;
    gap: 18px;
    margin-bottom: 52px;
  }

  .servicesIntro > p {
    max-width: 760px;
    margin: 0;
  }
}

@media (max-width: 1024px) {
  .servicesGrid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 760px) {
  .servicesSection {
    margin: 0 12px;
    border-radius: 24px;
    padding: 48px 18px 40px;
  }

  .container {
    width: 100%;
    padding: 0;
    margin: 0;
    border-radius: 0;
  }

  .servicesIntro {
    margin-bottom: 34px;
  }

  .servicesIntro h2 {
    font-size: 44px;
    line-height: 1.02;
  }

  .servicesIntro > p {
    font-size: 16px;
  }

  .introActions {
    display: grid;
    gap: 12px;
  }

  .introActions a:first-child {
    width: 100%;
  }

  .servicesGrid {
    grid-template-columns: 1fr;
    gap: 14px;
  }

  .serviceCard {
    min-height: auto;
    padding: 26px;
  }

  .serviceCard:hover {
    transform: none;
  }

  .serviceCard h3 {
    font-size: 31px;
  }

  .bigMark {
    font-size: 62px;
  }

  .featuredCard .bigMark {
    font-size: 68px;
  }

  .chips {
    min-height: 0;
  }

  .cardBottom {
    margin-top: 28px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .serviceCard {
    transition: none;
  }
}
