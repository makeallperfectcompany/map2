import Link from "next/link"; import { Container } from "@/components/ui/Container/Container"; import type { WordPressContent } from "@/content/services/wordpress"; import styles from "./WordPressDeliverablesSection.module.css";
type Props={content:WordPressContent["deliverables"]}; export default function WordPressDeliverablesSection({content}:Props){return <section className={styles.section}><Container className={styles.layout}><div className={styles.intro}><span className={styles.label}>Под ключ</span><h2>{content.title}</h2><p>{content.description}</p><Link className={styles.cta} href="#request">{content.cta}<span>→</span></Link></div><div className={styles.rows}>{content.items.map((item,index)=><article className={styles.row} key={item.title}><span>{String(index+1).padStart(2,"0")}</span><div><h3>{item.title}</h3><p>{item.text}</p></div></article>)}</div></Container></section>}
