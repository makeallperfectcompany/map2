import { Container } from "@/components/ui/Container/Container"; import grid from "@/styles/grid.module.css"; import type { WordPressContent } from "@/content/services/wordpress"; import styles from "./WordPressReadinessSection.module.css";
type Props={content:WordPressContent["readiness"]}; export default function WordPressReadinessSection({content}:Props){return <section className={styles.section}><Container><header className={styles.header}><span>{content.label}</span><h2>{content.title}</h2><p>{content.description}</p></header><div className={grid.grid2}>{content.items.map((item,index)=><article className={styles.card} key={item.title}><strong>{String(index+1).padStart(2,"0")}</strong><h3>{item.title}</h3><p>{item.text}</p></article>)}</div></Container></section>}
